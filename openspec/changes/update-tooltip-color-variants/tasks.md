## 1. Implementation
- [ ] 1.1 Update `tokens.json` — add variant color definitions
- [ ] 1.2 Update `tooltip.css` — add internal state variables and 7 variant modifier classes
- [ ] 1.3 Update `tooltip.js` — read `data-tooltip-variant`, apply class, add `setVariant()`, clean up on destroy
- [ ] 1.4 Update `index.html` — add color variants demo section
- [ ] 1.5 Verify all variants render correctly in browser
